# ruma

Backend Go sederhana menggunakan Gin dan MariaDB.

## Jalankan Lokal

```bash
go mod tidy
go run .
```

## Jalankan Dengan Docker

```bash
docker compose up --build
```

Endpoint:

- `GET /`
- `GET /health`
